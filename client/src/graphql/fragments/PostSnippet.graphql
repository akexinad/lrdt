fragment PostSnippet on Post {
    id
    title
    # text
    textSnippet # This is so we can minimize the data that we're returning
    creatorId
    createdAt
    updatedAt
    points
    voteStatus
    creator {
        id
        username
    }
}